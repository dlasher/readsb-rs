#[allow(dead_code)] // wired from globe data writer; pending Phase 5
#[repr(C, packed)]
pub struct BinCraft {
    pub hex: u32,
    pub seen: i32,
    pub lon: i32,
    pub lat: i32,
    pub baro_rate: i16, pub geom_rate: i16,
    pub baro_alt: i16, pub geom_alt: i16,
    pub nav_alt_mcp: u16, pub nav_alt_fms: u16,
    pub nav_qnh: i16, pub nav_heading: i16,
    pub squawk: u16, pub gs: i16, pub mach: i16,
    pub roll: i16, pub track: i16, pub track_rate: i16,
    pub mag_heading: i16, pub true_heading: i16,
    pub wind_dir: i16, pub wind_speed: i16,
    pub oat: i16, pub tat: i16,
    pub tas: u16, pub ias: u16,
    pub pos_rc: u16, pub messages: u16,
    pub category: u8, pub pos_nic: u8,
    pub nav_modes: u8, pub emergency: u8,
    pub airground: u8, pub nav_alt_src: u8,
    pub sil_type: u8, pub adsb_version: u8,
    pub callsign: [u8; 8],
    pub _pad: [u8; 32],
}
